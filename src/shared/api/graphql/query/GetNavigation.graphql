query GetNavigation($name: MenuAvailable!, $langcode: String!) {
    menu(name: $name, langcode: $langcode) {
        items {
            id
            title
            description
        }
    }
}
